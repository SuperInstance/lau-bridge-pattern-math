[package]
name = "lau-bridge-pattern-math"
version = "0.1.0"
edition = "2021"
description = "Bridges the Grand Pattern system with deep math crates: spectral theory, sheaves, category theory, optimal transport, free probability, PDEs"
license = "MIT"
repository = "https://github.com/SuperInstance/lau-bridge-pattern-math"

[dependencies]
nalgebra = "0.33"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
